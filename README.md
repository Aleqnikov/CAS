# CAS (Computer Algebra System)

> Система компьютерной алгебры, построенная на принципах абстрактной алгебры с использованием современного C++20.

## 📚 Оглавление
- [Философия проекта](#философия-проекта)
- [Архитектура](#архитектура)
- [Основные возможности](#основные-возможности)
- [Структура проекта](#структура-проекта)
- [Быстрый старт](#быстрый-старт)
- [Примеры использования](#примеры-использования)
- [Математические структуры](#математические-структуры)
- [Расширение системы](#расширение-системы)
- [Тестирование](#тестирование)

---

## 🎯 Философия проекта

**CAS** — это система компьютерной алгебры, основанная на строгом математическом подходе. Вместо традиционного объектно-ориентированного подхода, где операции встроены в типы, мы используем **подход абстрактной алгебры**:

- **Типы** = Математические множества (ℕ, ℤ, ℚ, ℤ/pℤ, полиномы)
- **Операции** = Отображения между множествами
- **Свойства** = Аксиомы, проверяемые через концепты C++20

### Ключевые принципы:

1. **Разделение данных и операций**: типы описывают только структуру данных, операции определяются отдельно
2. **Статическая проверка**: концепты C++20 гарантируют математическую корректность на этапе компиляции
3. **Композиция**: сложные структуры строятся из простых (полиномы над любым полем, факторструктуры)
4. **Ответственность программиста**: корректность математических свойств обеспечивается через систему тегов

> **Важно:** Если программист навешивает тег на операцию (например, `Associative`), но свойство не выполняется — программа скомпилируется, но будет работать некорректно. Система доверяет программисту!

---

## 🏗️ Архитектура

### Трехуровневая система:
```
lib/src/core/
├── abstract/              # Абстрактная алгебра (математика)
│   ├── structures/        # Концепты алгебраических структур
│   ├── transformations/   # Отображения и операции
│   └── types/             # Базовые типы данных (только структура)
└── realization/           # Конкретные реализации
    ├── Natural/           # Натуральные числа ℕ
    ├── Integer/           # Целые числа ℤ
    ├── Rational/          # Рациональные числа ℚ
    ├── Polynomial/        # Полиномы F[x]
    └── deductionclass/    # Факторструктуры (ℤ/pℤ и т.д.)
```

### Иерархия абстракций:
```
                    Mapping (отображение)
                          ↓
        BinaryOperation / UnaryOperation (операции)
                          ↓
        Конкретные операции с тегами
        (Add, Mul, ... + Associative, Commutative, ...)
                          ↓
              Классы-обертки с операторами
              (N, Z, Q, P<T>, Zp<p>)
                          ↓
         Проверка через концепты (Field, Ring, ...)
```

---

## ⚡ Основные возможности

### ✅ Числовые типы:
- **N** — Натуральные числа (произвольной точности)
- **Z** — Целые числа (произвольной точности)
- **Q** — Рациональные числа (автоматическое сокращение)
- **Zp<p>** — Конечные поля ℤ/pℤ (p простое)

### ✅ Алгебраические структуры:
- **Полиномы** над любым полем: `P<Q>`, `P<Zp<5>>`
- **Факторкольца**: `FactorRing<R, I>` при наличии идеала
- **Факторполя**: `FactorField<R, I>` когда идеал максимален

### ✅ Операции:
- Арифметика: `+`, `-`, `*`, `/`, `%`
- НОД, НОК
- Производные полиномов
- Деление полиномов с остатком
- Обратные элементы в конечных полях

### ✅ Compile-time проверки:
```cpp
// ❌ Ошибка компиляции - N не является полем
Polynomial<N> poly;  // COMPILE ERROR!

// ✅ Компилируется - Q является полем
Polynomial<Q> poly;

// ✅ Компилируется - Zp<5> является полем
Polynomial<Zp<5>> poly;

// Проверка структур в тестах
static_assert(Field<Q, Q::AdditionOp, Q::MultiplicationOp>);
static_assert(!Field<Z, Z::AdditionOp, Z::MultiplicationOp>);
```

---

## 📁 Структура проекта
```
CAS/
├── CMakeLists.txt                    # Корневой CMake
├── README.md                         # Этот файл
├── TODO.md                           # Планы развития
│
├── lib/
│   ├── src/
│   │   ├── core/
│   │   │   ├── README.md             # Документация ядра
│   │   │   │
│   │   │   ├── abstract/             # === АБСТРАКТНАЯ АЛГЕБРА ===
│   │   │   │   │
│   │   │   │   ├── structures/       # Алгебраические структуры
│   │   │   │   │   ├── axioms.h            # Базовые тэги (Associative, Commutative, ...)
│   │   │   │   │   ├── groups.h            # Магмы → Моноиды → Группы
│   │   │   │   │   ├── rings.h             # Кольца → Области → Поля
│   │   │   │   │   ├── commuttative_algebra.h  # Идеалы (Ideal, MaximalIdeal)
│   │   │   │   │   └── factor.h            # Факторструктуры (FactorRing, FactorField)
│   │   │   │   │
│   │   │   │   ├── transformations/  # Отображения и операции
│   │   │   │   │   ├── mapping.h           # Базовое отображение f: A → B
│   │   │   │   │   ├── operations/
│   │   │   │   │   │   ├── binary.h        # Бинарные операции *: T×T → T
│   │   │   │   │   │   └── unary.h         # Унарные операции f: T → T
│   │   │   │   │   └── morphism/
│   │   │   │   │       ├── homomorphism.h  # Гомоморфизмы
│   │   │   │   │       └── isomorphism.h   # Изоморфизмы
│   │   │   │   │
│   │   │   │   └── types/             # Базовые типы (только данные!)
│   │   │   │       ├── natural.h           # struct Natural
│   │   │   │       ├── integer.h           # struct Integer
│   │   │   │       ├── rational.h          # struct Rational
│   │   │   │       └── polynomial.h        # struct Polynomial<T>
│   │   │   │
│   │   │   └── realization/           # === КОНКРЕТНЫЕ РЕАЛИЗАЦИИ ===
│   │   │       │
│   │   │       ├── README.md          # Документация реализаций
│   │   │       │
│   │   │       ├── Natural/           # Натуральные числа ℕ
│   │   │       │   ├── operations.h        # Add, Mul, Div, Gcd, ... (с тегами)
│   │   │       │   └── N.h                 # Класс-обертка с операторами
│   │   │       │
│   │   │       ├── Integer/           # Целые числа ℤ
│   │   │       │   ├── operations.h        # Add, Mul, Neg, ... (с тегами)
│   │   │       │   └── Z.h                 # Класс-обертка с операторами
│   │   │       │
│   │   │       ├── Rational/          # Рациональные числа ℚ
│   │   │       │   ├── operations.h        # Add, Mul, Div, Reduce, ...
│   │   │       │   └── Q.h                 # Класс-обертка с операторами
│   │   │       │
│   │   │       ├── Polynomial/        # Полиномы F[x]
│   │   │       │   ├── operations.h        # Add, Mul, Div, Derivative, ...
│   │   │       │   └── P[x].h              # Класс-обертка P<T>
│   │   │       │
│   │   │       └── deductionclass/    # Факторструктуры
│   │   │           ├── principal_ideal_z.h # Идеал nℤ в ℤ
│   │   │           └── pZ.h                # Zp<p> = ℤ/pℤ (конечные поля)
│   │   │
│   │   ├── Exceptions/
│   │   │   └── UniversalStringException.h  # Обработка ошибок
│   │   │
│   │   └── main.cpp                   # Точка входа
│   │
│   └── tests/                         # === ТЕСТИРОВАНИЕ (Google Test) ===
│       ├── CMakeLists.txt
│       ├── core/                      # Тесты ядра
│       │   ├── natural_test.cpp            # Тесты N (30+ тестов)
│       │   ├── integer_test.cpp            # Тесты Z (30+ тестов)
│       │   ├── rational_test.cpp           # Тесты Q (30+ тестов)
│       │   ├── polynomial_test.cpp         # Тесты P<T> (30+ тестов)
│       │   └── deductions_test.cpp         # Тесты Zp<p> (30+ тестов)
│       │
│       └── [legacy tests]/            # Старые тесты (для совместимости)
│
└── cmake-build-debug/                 # Артефакты сборки
    ├── lib/
    │   ├── libcas.a                   # Библиотека CAS
    │   ├── libgtest.a                 # Google Test
    │   └── tests/
    │       └── run_tests              # Исполняемый файл тестов
    └── ...
```

---

## 🚀 Быстрый старт

### Требования:
- **C++20** компилятор:
    - GCC ≥ 10
    - Clang ≥ 10
    - MSVC ≥ 2019 (16.8+)
- **CMake** ≥ 3.20
- **Google Test** (загружается автоматически)

### Сборка:
```bash
# Клонирование репозитория
git clone https://github.com/your-repo/CAS.git
cd CAS

# Сборка
mkdir build && cd build
cmake ..
make -j$(nproc)
```

### Запуск тестов:
```bash
# Все тесты (~150 тестов)
./lib/tests/run_tests

# Конкретная группа
./lib/tests/run_tests --gtest_filter=Zp*

# С подробным выводом времени
./lib/tests/run_tests --gtest_print_time=1
```

### Использование в коде:
```cpp
#include "core/realization/Natural/N.h"
#include "core/realization/Rational/Q.h"
#include "core/realization/Polynomial/P[x].h"

int main() {
    // Работа с рациональными числами
    Q a(Z({1}), N({2}));  // 1/2
    Q b(Z({1}), N({3}));  // 1/3
    
    std::cout << (a + b).toString();  // 5/6
    
    // Полиномы над Q
    P<Q> poly({a, b});  // (1/2) + (1/3)x
    auto deriv = poly.derivative();
    
    return 0;
}
```

---

## 💡 Примеры использования

### 1. Натуральные числа (произвольной точности):
```cpp
N a({3, 2, 1});  // 123 (little-endian: [3, 2, 1] → 3 + 2·10 + 1·100)
N b({5, 4});     // 45

auto sum = a + b;          // 168
auto product = a * b;      // 5535
auto quotient = a / b;     // 2
auto remainder = a % b;    // 33
auto gcd_val = N::gcd(a, b);  // 3
auto lcm_val = N::lcm(a, b);  // 1845

std::cout << sum.toString();  // "168"
```

### 2. Целые числа:
```cpp
Z positive({3, 2, 1}, false);  // 123
Z negative({5, 4}, true);      // -45

auto sum = positive + negative;  // 78
auto product = positive * negative;  // -5535
auto diff = positive - negative;     // 168

// Унарный минус
auto neg = -positive;  // -123
```

### 3. Рациональные числа:
```cpp
Q half(Z({1}), N({2}));      // 1/2
Q third(Z({1}), N({3}));     // 1/3

auto sum = half + third;     // 5/6 (автоматическое приведение к общему знаменателю)
auto product = half * third; // 1/6
auto quotient = half / third; // 3/2

// Сравнение
bool equal = (half == Q(Z({3}), N({6})));  // true (1/2 == 3/6)
```

### 4. Полиномы над рациональными числами:
```cpp
// 1 + 2x + 3x² над ℚ
P<Q> poly({
    Q(Z({1}), N({1})),  // 1
    Q(Z({2}), N({1})),  // 2
    Q(Z({3}), N({1}))   // 3
});

// Операции
auto deriv = poly.derivative();     // 2 + 6x
auto square = poly * poly;          // (1+2x+3x²)²
auto sum = poly + poly;             // 2 + 4x + 6x²

// Деление полиномов
P<Q> divisor({Q(1,1), Q(1,1)});    // 1 + x
auto quotient = poly / divisor;
auto remainder = poly % divisor;

std::cout << poly.toString();       // "3x^2 + 2x + 1"
std::cout << "degree: " << poly.degree();  // 2
```

### 5. Конечные поля ℤ/pℤ:
```cpp
// ℤ/5ℤ - поле из 5 элементов {0, 1, 2, 3, 4}
Zp<5> a(makeZ(3));  // 3
Zp<5> b(makeZ(4));  // 4

// Арифметика по модулю 5
auto sum = a + b;    // 7 mod 5 = 2
auto prod = a * b;   // 12 mod 5 = 2
auto diff = a - b;   // -1 mod 5 = 4

// ДЕЛЕНИЕ работает! (т.к. это поле)
auto inv = a / b;    // 3 · 4⁻¹ mod 5 = 3 · 4 = 12 mod 5 = 2

// Отрицательные числа автоматически приводятся
Zp<7> neg(makeZ(-3));  // -3 mod 7 = 4

std::cout << sum.toString();  // "2"
```

### 6. Полиномы над конечным полем (криптография!):
```cpp
// Полиномы над ℤ/5ℤ
Zp<5> a(makeZ(2)), b(makeZ(3)), c(makeZ(1));

P<Zp<5>> poly({a, b, c});  // 2 + 3x + x² над ℤ/5ℤ

auto square = poly * poly;  // Все коэффициенты автоматически по mod 5
auto deriv = poly.derivative();

// Используется в эллиптических кривых, конечных полях Галуа
```

### 7. Проверка алгебраических свойств:
```cpp
// Compile-time проверки
static_assert(Monoid<N, N::AdditionOp>);  // ℕ - моноид по сложению
static_assert(!Group<N, N::AdditionOp>);  // ℕ - НЕ группа (нет обратного)

static_assert(AbelianGroup<Z, Z::AdditionOp>);  // ℤ - абелева группа по +
static_assert(UnitaryRing<Z, Z::AdditionOp, Z::MultiplicationOp>);  // ℤ - кольцо

static_assert(Field<Q, Q::AdditionOp, Q::MultiplicationOp>);  // ℚ - поле
static_assert(Field<Zp<5>, Zp<5>::AdditionOp, Zp<5>::MultiplicationOp>);  // ℤ/5ℤ - поле

// Полиномы НЕ являются полем
static_assert(!Field<P<Q>, P<Q>::AdditionOp, P<Q>::MultiplicationOp>);
```

---

## 🔬 Математические структуры

### Иерархия групп:
```
Magma (магма)
  └─ Замкнутость: a ∗ b ∈ G
       ↓
Semigroup (полугруппа)
  └─ + Ассоциативность: (a ∗ b) ∗ c = a ∗ (b ∗ c)
       ↓
Monoid (моноид)
  └─ + Нейтральный элемент: ∃e: a ∗ e = e ∗ a = a
       ↓
Group (группа)
  └─ + Обратный элемент: ∀a ∃a⁻¹: a ∗ a⁻¹ = e
       ↓
AbelianGroup (абелева группа)
  └─ + Коммутативность: a ∗ b = b ∗ a
```

**Примеры:**
- (ℕ, +) — моноид (есть 0, но нет обратных)
- (ℤ, +) — абелева группа
- (ℚ*, ·) — абелева группа (без нуля)

### Иерархия колец:
```
Ring (кольцо)
  └─ (R, +) — абелева группа
  └─ (R, ·) — полугруппа
  └─ Дистрибутивность
       ↓
UnitaryRing (кольцо с единицей)
  └─ + (R, ·) — моноид
       ↓
IntegralDomain (область целостности)
  └─ + Нет делителей нуля
       ↓
Field (поле)
  └─ + (R\{0}, ·) — группа (все ненулевые обратимы)
```

**Примеры:**
- ℤ — кольцо с единицей, область целостности
- ℚ — поле
- ℤ/6ℤ — кольцо (НЕ область: 2·3 ≡ 0)
- ℤ/5ℤ — поле (5 простое!)

### Концепты в коде:
```cpp
// Группа
template<typename T, typename Op>
concept Group = 
    Monoid<T, Op> &&           // Моноид
    HasInverse<Op>;            // + обратный элемент

// Кольцо
template<typename T, typename AddOp, typename MulOp>
concept Ring = 
    AbelianGroup<T, AddOp> &&  // По + абелева группа
    Semigroup<T, MulOp> &&     // По · полугруппа
    IsDistributive<MulOp, AddOp, T>;  // Дистрибутивность

// Поле
template<typename T, typename AddOp, typename MulOp>
concept Field = 
    UnitaryRing<T, AddOp, MulOp> &&  // Кольцо с 1
    AbelianGroup<T, MulOp>;          // По · группа (с делением!)
```

### Идеалы и факторструктуры:
```cpp
// Идеал I в кольце R
template<typename I, typename R>
concept Ideal = requires {
    // I — подгруппа R по сложению
    requires Subgroup<I, R>;
    
    // Замкнутость относительно умножения на R
    // ∀r ∈ R, ∀a ∈ I: r·a ∈ I ∧ a·r ∈ I
};

// Максимальный идеал
template<typename I, typename R>
concept MaximalIdeal = 
    Ideal<I, R> && 
    std::derived_from<I, Maximal>;  // Тег максимальности

// Факторкольцо R/I
template<typename R, typename I>
requires Ideal<I, R>
class FactorRing { /* ... */ };

// Факторполе R/I (ТОЛЬКО если I максимален!)
template<typename R, typename I>
requires MaximalIdeal<I, R>
class FactorField : public FactorRing<R, I> { /* ... */ };
```

**Теорема:** R/I — поле ⟺ I — максимальный идеал

**Примеры:**
- ℤ/5ℤ — поле (5 простое → идеал (5) максимален)
- ℤ/6ℤ — НЕ поле (6 составное → идеал (6) не максимален)
- ℚ[x]/(x²-2) — поле ℚ(√2)

---

## 🛠️ Расширение системы

### Как добавить новый числовой тип:

**Шаг 1:** Определите базовый тип (только данные!)
```cpp
// В core/abstract/types/complex.h
struct Complex {
    Q real;
    Q imag;
    
    Complex(Q r, Q i) : real(r), imag(i) {}
};
```

**Шаг 2:** Создайте операции с тегами
```cpp
// В core/realization/Complex/operations.h
namespace ComplexOps {

class Add : public BinaryOperation<Add, Complex>,
            public Associative,
            public Commutative,
            public Identity,
            public Inverse
{
public:
    static Complex calc(Complex a, Complex b) {
        return Complex(a.real + b.real, a.imag + b.imag);
    }
};

class Mul : public BinaryOperation<Mul, Complex>,
            public Associative,
            public Commutative,
            public Identity
{
public:
    static Complex calc(Complex a, Complex b) {
        // (a+bi)(c+di) = (ac-bd) + (ad+bc)i
        Q real_part = a.real * b.real - a.imag * b.imag;
        Q imag_part = a.real * b.imag + a.imag * b.real;
        return Complex(real_part, imag_part);
    }
};

} // namespace ComplexOps
```

**Шаг 3:** Создайте класс-обертку
```cpp
// В core/realization/Complex/C.h
class C {
private:
    Complex value;

public:
    using SetType = Complex;
    using AdditionOp = ComplexOps::Add;
    using MultiplicationOp = ComplexOps::Mul;

    C(Complex v) : value(std::move(v)) {}
    C(Q real, Q imag) : value(real, imag) {}

    C operator+(const C& other) const {
        return C(ComplexOps::Add::execute(value, other.value));
    }

    C operator*(const C& other) const {
        return C(ComplexOps::Mul::execute(value, other.value));
    }

    C operator/(const C& other) const {
        // (a+bi)/(c+di) = (a+bi)(c-di)/(c²+d²)
        Q denom = other.value.real * other.value.real + 
                  other.value.imag * other.value.imag;
        Q real_part = (value.real * other.value.real + 
                       value.imag * other.value.imag) / denom;
        Q imag_part = (value.imag * other.value.real - 
                       value.real * other.value.imag) / denom;
        return C(real_part, imag_part);
    }

    static C zero() { return C(Q::zero(), Q::zero()); }
    static C identity() { return C(Q::identity(), Q::zero()); }

    bool isNegative() const { return false; }  // Комплексные не упорядочены

    const Complex& get() const { return value; }
    
    std::string toString() const {
        return value.real.toString() + " + " + 
               value.imag.toString() + "i";
    }
};

// Проверка что это поле!
static_assert(Field<C, C::AdditionOp, C::MultiplicationOp>);
```

**Шаг 4:** Используйте!
```cpp
C a(Q(3,1), Q(4,1));  // 3 + 4i
C b(Q(1,1), Q(2,1));  // 1 + 2i

auto sum = a + b;      // 4 + 6i
auto product = a * b;  // -5 + 10i

// Полиномы над комплексными числами!
P<C> poly({a, b});
```

---

## 🧪 Тестирование

### Структура тестов:
```
lib/tests/
├── core/                    # Тесты ядра (~150 тестов)
│   ├── natural_test.cpp          # N: 30+ тестов
│   ├── integer_test.cpp          # Z: 30+ тестов
│   ├── rational_test.cpp         # Q: 30+ тестов
│   ├── polynomial_test.cpp       # P<T>: 30+ тестов
│   └── deductions_test.cpp       # Zp<p>: 30+ тестов
└── [legacy]/                # Старые тесты
```

### Запуск тестов:
```bash
# Все тесты
./lib/tests/run_tests

# Фильтрация
./lib/tests/run_tests --gtest_filter=NaturalAdd*
./lib/tests/run_tests --gtest_filter=Zp*

# С временем выполнения
./lib/tests/run_tests --gtest_print_time=1

# Список тестов
./lib/tests/run_tests --gtest_list_tests
```

### Пример теста:
```cpp
TEST(NaturalAdd1, Basic) {
    N a({7, 9});  // 97
    N b({5, 2});  // 25
    auto c = a + b;
    EXPECT_EQ(c.toString(), "122");
}

TEST(ZpDiv1, InverseElement) {
    Zp<5> a(makeZ(3));
    Zp<5> b(makeZ(2));
    auto c = a / b;  // 3 · 2⁻¹ mod 5 = 3 · 3 = 9 mod 5 = 4
    EXPECT_EQ(c.toString(), "4");
}
```

### Покрытие:

- ✅ Все арифметические операции
- ✅ Граничные случаи (ноль, единица, переполнение)
- ✅ Проверка алгебраических свойств через `static_assert`
- ✅ Обработка ошибок (деление на ноль, отрицательные остатки)

---

## 📖 Документация

### Основные файлы документации:

- `README.md` (этот файл) — общий обзор
- `lib/src/core/README.md` — подробная документация ядра
- `lib/src/core/realization/README.md` — документация реализаций
- `TODO.md` — планы развития

### Дополнительные ресурсы:

- Концепты C++20: [cppreference.com/concepts](https://en.cppreference.com/w/cpp/concepts)
- CRTP паттерн: [Wikipedia CRTP](https://en.wikipedia.org/wiki/Curiously_recurring_template_pattern)
- Абстрактная алгебра: [Abstract Algebra (Dummit & Foote)](https://www.wiley.com/en-us/Abstract+Algebra%2C+3rd+Edition-p-9780471433347)

---

## 🚧 TODO

### Ближайшие планы:

- [ ] Матрицы над полями
- [ ] Векторные пространства
- [ ] Расширения полей (ℚ(√2), конечные поля ℙ^n)
- [ ] Эллиптические кривые
- [ ] Система линейных уравнений
- [ ] Факторизация полиномов
- [ ] GUI интерфейс

### Оптимизации:

- [ ] Быстрое возведение в степень (для полиномов)
- [ ] Алгоритм Карацубы для умножения
- [ ] FFT для умножения полиномов
- [ ] Параллельные вычисления


---
---

**⭐ Если проект полезен — поставьте звезду!**

**🐛 Нашли баг? Создайте issue!**

**💡 Есть идеи? Pull requests приветствуются!**